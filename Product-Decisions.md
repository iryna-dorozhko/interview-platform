# Product Decisions

Журнал важливих продуктових і архітектурних рішень для AI-платформи співбесід.

**Як користуватися:** кожне нове рішення додавайте в кінець файлу за шаблоном нижче. Коротко, по суті — без довгих есе.

---

## Шаблон запису

```markdown
### PD-NNN. Назва рішення

**Дата:** YYYY-MM-DD  
**Статус:** прийнято | переглянуто | скасовано

**Рішення:** …

**Чому саме так:** …

**Альтернативи:**
- …
- …

**Чому відмовилися:** …
```

---

## Записи

### PD-001. Чистий старт репозиторію

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** Новий репозиторій з нуля; код з проєкту `agent` не копіюється.

**Чому саме так:** MVP має вузький scope і простішу структуру. Копіювання старого коду тягне зайві залежності, патерни та борг, які не потрібні для скелета.

**Альтернативи:**
- Форк або копія репозиторію `agent`
- Спільний monorepo з `agent`

**Чому відмовилися:** `agent` ширший за потреби MVP; рефакторинг копії дорожчий за свідомий чистий старт з успадкуванням лише ідей (агенти, orchestrator, LLM-шар).

---

### PD-002. Scope MVP: login → prep → кімната → звіт

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** Перший реліз покриває повний вертикальний сценарій: вхід → анкетування → live-співбесіда → фінальний звіт.

**Чому саме так:** Можна пройти end-to-end сценарій і перевірити цінність продукту без побічних фіч.

**Альтернативи:**
- Лише prep без live-кімнати
- Лише кімната з ручним введенням профілів
- Повний функціонал старого `agent`

**Чому відмовилися:** Prep-only не демонструє головну цінність (спільна співбесіда з агентами). Повний `agent` — занадто довго до першої перевірки. Ручні профілі без prep спрощують demo, але не тестують AI-збір даних.

---

### PD-003. Свідомі виключення з MVP

**Дата:** 2026-07-01  
**Статус:** переглянуто

**Рішення:** Поза scope: Issues/теми, бібліотека анкет, історія співбесід, відмовні листи, HR control rail, стрімінг LLM, автотести (окрім build + ручний сценарій).

**Чому саме так:** Кожна з цих фіч — окремий продуктовий шар. Для скелета важливіше стабільний happy path, ніж повнота enterprise-функцій.

**Альтернативи:**
- Issues з самого початку (структурована дискусія)
- Збережені профілі для повторного використання
- Стрімінг відповідей агентів у UI

**Чому відмовилися:** Issues і control rail ускладнюють UX і orchestrator. Saved profiles — окрема модель даних і UI. Стрімінг додає складність Socket/UI без критичної цінності для локального MVP.

> **Примітка (2026-07-03):** oMLX знято з виключень як локальний LLM-провайдер для dev — див. PD-019. Стрімінг LLM лишається поза scope.  
> **Примітка (2026-07-22):** відмовні / decision-листи реалізовані через HR-рішення після звіту і Dialogs — див. PD-030. Issues, control rail, стрімінг LLM, video/audio recording, SMTP, PDF звіту, admin/org multi-tenancy, i18n — досі поза scope.

### PD-004. Повний prep-цикл з підтвердженням профілю

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** AI веде діалог → сигнал `readyForConfirmation` → користувач підтверджує → LLM генерує JSON-профіль → prep-сесія закривається.

**Чому саме так:** Людина контролює фіксацію даних перед співбесідою; агенти в кімнаті працюють з перевіреною структурою, а не з сирим чатом.

**Альтернативи:**
- Одразу писати JSON після кожного повідомлення
- Ручне редагування полів профілю в формі
- Без підтвердження — автоматичний перехід у кімнату

**Чому відмовилися:** Постійний JSON — шум і помилки парсингу. Ручні форми — інший UX і дублювання prep-чату. Автоперехід без підтвердження ризикує некоректними профілями в live-чаті.

---

### PD-005. Профілі прив'язані до співбесіди

**Дата:** 2026-07-01  
**Статус:** переглянуто → **PD-020**, **PD-021**, **PD-022**

**Рішення:** `Profile` зберігається на рівні `Interview`, без окремої бібліотеки `SavedProfile`.

**Чому саме так:** Мінімальна модель даних; один interview — один набір профілів кандидата й компанії.

**Альтернативи:**
- Глобальна бібліотека профілів користувача
- Профіль компанії на рівні HR-акаунта, перевикористання між співбесідами

**Чому відмовилися:** Бібліотека потребує версіонування, вибору профілю в UI і прав доступу. Для MVP достатньо «одна співбесіда — один контекст».

> **Примітка (2026-07-16+):** компанія — на `Vacancy` + глобальний `HrCompanyProfile` (snapshot); кандидат — self-service «Моя анкета» з перевикористанням. Ідея «без окремої SavedProfile-бібліотеки» збережена, але привʼязка лише до Interview більше не описує домен.

---

### PD-006. Учасники live-кімнати: 2 людини + 3 агенти

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** У кімнаті одночасно п'ять учасників: HR, кандидат, Candidate Agent, Company Agent, Arbiter.

**Чому саме так:** Агенти представляють інтереси сторін і модерують дискусію; люди залишаються головними спікерами.

**Альтернативи:**
- Лише Arbiter + люди (без side-agents)
- Один універсальний агент замість трьох
- Агенти лише в prep, у кімнаті — тільки люди

**Чому відмовилися:** Без side-agents губиться асиметрія «інтереси кандидата vs компанії». Один агент не розділяє ролі. Кімната без агентів не перевіряє головну гіпотезу продукту.

---

### PD-007. Порядок агентів у кімнаті

**Дата:** 2026-07-01  
**Статус:** переглянуто → **PD-026** (2026-07-16)

**Рішення:** Після повідомлення людини: Candidate Agent → Company Agent → Arbiter. Максимум одне публічне повідомлення від кожного агента за хід.

**Чому саме так:** Спочатку позиція кандидата, потім компанії, потім синтез і модерація Arbiter'ом — логічна арка одного «раунду» дискусії.

**Альтернативи:**
- Arbiter → Company → Candidate
- Паралельні виклики агентів
- Динамічний вибір, хто відповідає

**Чому відмовилися:** Arbiter першим не має контексту від сторін. Паралельність ускладнює порядок у чаті та batch на клієнті. Динамічний вибір — зайва логіка для MVP.

> **Примітка (2026-07-16):** фіксований порядок замінено моделлю Arbiter-conductor з прихованими командами і selective run — див. PD-026.

---

### PD-008. Prep — HTTP; кімната — Socket.IO

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** Анкетування через синхронні REST-запити; live-чат — WebSocket (Socket.IO) з async orchestrator на backend.

**Чому саме так:** Prep — request/response з очікуваною паузою на LLM. Кімната — realtime між двома клієнтами і batch повідомлень від агентів.

**Альтернативи:**
- Усе через WebSocket
- Усе через HTTP long-polling
- SSE для стріму відповідей агентів

**Чому відмовилися:** WebSocket для prep — зайва складність. Long-polling гірший UX у кімнаті. SSE/стрімінг свідомо виключено з MVP (PD-003).

---

### PD-009. Технологічний стек

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** npm workspaces monorepo; Vue 3 + Vite + Pinia; Express + TypeScript; PostgreSQL 16 + Prisma; Socket.IO; JWT.

**Чому саме так:** Знайомий стек команди, швидкий старт, достатньо для локального MVP і Docker Compose.

**Альтернативи:**
- Next.js fullstack
- Python (FastAPI) backend
- SQLite замість PostgreSQL
- Session cookies замість JWT

**Чому відмовилися:** Next.js — інший розподіл frontend/backend для socket-heavy кімнати. Python — розрив з TypeScript на обох кінцях. SQLite гірше для concurrent room + dockerized postgres уже в плані. Session cookies ускладнюють SPA + окремий frontend container.

---

### PD-010. LLM: Ollama (локально) + LiteLLM (хмара)

**Дата:** 2026-07-01  
**Статус:** переглянуто → **PD-019** (2026-07-03)

**Рішення:** Плагінований `LlmProvider`; перемикання через `LLM_PROVIDER=ollama|litellm` у `.env`. Ollama на хості, не в Docker.

**Чому саме так:** Локальна розробка без API keys; можливість перемкнутися на хмарну модель для якості/швидкості без зміни коду агентів.

**Альтернативи:**
- Тільки Ollama
- Тільки OpenAI-compatible API
- oMLX як третій провайдер у MVP
- Ollama в Docker-контейнері

**Чому відмовилися:** Один провайдер обмежує сценарії тестування. oMLX — поза scope MVP. Ollama в Docker на macOS часто гірша DX (GPU, розмір образу); `host.docker.internal` простіший для dev.

> **Примітка (2026-07-03):** На Дні 2 реалізовано інший стек — див. PD-019. Ідея плагінованого `LlmProvider` і перемикання через `.env` збережена.

---

### PD-011. Підхід до скелета: vertical slice + модульність

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** Hybrid: вертикальні зрізи (auth → prep → room → report) + модульні сервіси (`agents/`, `llm/`, `routes/`).

**Чому саме так:** Кожен день плану дає робочий шматок продукту; модулі не злипаються в один файл до кінця проєкту.

**Альтернативи:**
- Big-bang: спочатку вся backend-архітектура, потім UI
- Тільки горизонтальні шари без end-to-end перевірок
- Один файл «на все» для швидкості

**Чому відмовилися:** Big-bang пізно показує цінність. Горизонтальні шари без зрізів — ризик несумісних частин на дні 15+. Монолітний файл не масштабується до 3 агентів і socket.

---

### PD-012. Мова продукту — українська

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** UI, промпти агентів, повідомлення про помилки та звіти — українською.

**Чому саме так:** Цільова аудиторія і сценарій співбесід локальні; єдина мова зменшує змішування в промптах і звітах.

**Альтернативи:**
- Англійська для всього
- i18n з першого дня

**Чому відмовилися:** i18n — зайва робота для MVP. Англійська не відповідає заявленому контексту продукту.

---

### PD-013. Автентифікація: окремі ролі HR і кандидата

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** Ролі `HR` і `CANDIDATE`; окремі флоу входу/реєстрації; JWT у заголовку `Authorization`.

**Чому саме так:** Різні кабінети, різні дії (створити співбесіду vs приєднатися за кодом); проста RBAC на backend.

**Альтернативи:**
- Один тип користувача + вибір ролі після логіну
- Magic link без пароля
- Спільний логін з перемикачем «я HR / я кандидат»

**Чому відмовилися:** Вибір ролі на одному акаунті плутає модель даних. Magic link — зайва інфра для локального MVP. Перемикач ролі на одній сторінці гірший UX, ніж окремі entry points.

---

### PD-014. Приєднання кандидата за 6-символьним кодом

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** HR створює співбесіду → унікальний код (6 символів) → кандидат вводить код для join.

**Чому саме так:** Простий офлайн-обмін між HR і кандидатом без email-інвайтів у MVP.

**Альтернативи:**
- Email-запрошення з посиланням
- Пошук співбесіди за назвою компанії
- QR-код

**Чому відмовилися:** Email/посилання потребує пошти, шаблонів, терміну дії URL. Публічний пошук — витік даних. QR — зайвий UI без мобільного фокусу.

---

### PD-015. Одна активна співбесіда на HR і на кандидата

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** У MVP користувач не веде паралельно кілька активних інтервʼю.

**Чому саме так:** Спрощує дашборд, статуси і перевірки «чи зайнятий кандидат».

**Альтернативи:**
- Необмежена кількість паралельних співбесід
- Черга співбесід у UI

**Чому відмовилися:** Паралельні інтервʼю — складні state machine і room routing. Черга — фіча наступних ітерацій.

> **Примітка (2026-07-17+):** кандидат може мати confirmed анкету, match і PENDING заявку паралельно з історією ended-співбесід; обмеження «одна **активна** Interview» лишається enforced у join / create-from-application.

---

### PD-016. Завершення співбесіди: ініціатива HR

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** Arbiter може запропонувати завершення; фінальне рішення — за HR (`POST /interviews/:id/end` → генерація звіту).

**Чому саме так:** HR — сторона найму; кандидат не повинен одноосібно закривати процес із звітом hire/maybe/reject.

**Альтернативи:**
- Автозавершення після пропозиції Arbiter
- Завершення за згодою обох людей
- Таймаут без участі HR

**Чому відмовилися:** Автозавершення ризикує передчасним звітом. Двостороння згода ускладнює UX. Таймаут — непрозора бізнес-логіка для MVP.

---

### PD-017. Порядок розробки: спочатку HR, потім кандидат

**Дата:** 2026-07-01  
**Статус:** прийнято

**Рішення:** 22-денний план: інфра → HR-кабінет (повний цикл) → кабінет кандидата → спільна кімната → звіт і Docker.

**Чому саме так:** HR створює співбесіду й код; без HR-флоу немає що тестувати з боку кандидата. Патерни prep (чат, профіль, confirm) відпрацьовуються на Company Agent і копіюються на Candidate Agent.

**Альтернативи:**
- Паралельна розробка HR і кандидата
- Спочатку кімната з mock-профілями
- Backend-first без UI до дня 15

**Чому відмовилися:** Паралельність дублює роботу над prep UI. Mock-профілі відкладають перевірку prep-агентів. Backend-only пізно ловить UX-проблеми.

---

### PD-018. Розгортання: Docker Compose

**Дата:** 2026-07-01  
**Статус:** переглянуто → **PD-034** (2026-07-24)

**Рішення:** `docker compose up` піднімає postgres + backend + frontend; LLM (omlx) залишається на хості, не в Docker.

**Чому саме так:** Один стандартний спосіб запуску для перевірки MVP; ізоляція залежностей БД і сервісів.

**Альтернативи:**
- Локальний запуск без Docker
- Kubernetes / хмарний deploy у MVP
- Managed DB (RDS тощо)

**Чому відмовилися:** Без Docker складніше відтворити середовище. K8s — overkill. Managed DB — зайва вартість і налаштування для скелета.

> **Примітка:** у поточному `docker-compose.yml` лише Postgres 16; backend і frontend запускаються локально (Vite/npm). LLM на хості — без змін.

---

### PD-019. LLM: oMLX (локально) + Gemini (хмара)

**Дата:** 2026-07-03  
**Статус:** прийнято

**Рішення:** Плагінований `LlmProvider`; перемикання через `LLM_PROVIDER=omlx|gemini` у `.env`.

- **Локально:** oMLX (`omlx serve`), OpenAI-compatible API `/v1/chat/completions`; модель за замовчуванням `Qwen2.5-7B-Instruct-4bit`; опційно `OMLX_API_KEY` при auth на сервері.
- **Хмара:** Google Gemini через офіційний SDK `@google/generative-ai` (не LiteLLM); модель за замовчуванням `gemini-2.0-flash`.
- oMLX запускається окремим процесом на хості, не в Docker. API keys не комітити.

**Чому саме так:** На машині розробника вже є oMLX з готовою моделлю на Apple Silicon; OpenAI-compatible API спрощує клієнтський код. Gemini напряму — менше залежностей на День 2; інтерфейс `LlmProvider` готовий до додавання LiteLLM/Ollama пізніше без зміни агентів.

**Альтернативи:**
- Залишити PD-010 (Ollama + LiteLLM) без змін
- LiteLLM як проксі до Gemini замість офіційного SDK
- Тільки oMLX без хмарного fallback
- Ollama в Docker-контейнері

**Чому відмовилися:** Ollama потребувала б окремого setup; LiteLLM — зайвий шар для першого вертикального зрізу. Тільки oMLX обмежує demo без локальної GPU. oMLX у Docker на macOS — гірша DX, ніж `omlx serve` на хості.

**Реалізація:** День 2 — `backend/src/llm/`, `POST /api/llm/complete`, `npm run llm:test`. День 3 — endpoint захищено `requireAuth` + `requireHr`.

> **Примітка (2026-07-15+):** додано провайдери `openai` і `cursor-acp` — див. PD-032. Ідея плагінованого `LlmProvider` збережена.

---

### PD-020. Vacancy ≠ Interview

**Дата:** 2026-07-08  
**Статус:** прийнято

**Рішення:** Окрема сутність `Vacancy` (анкета посади) з prep і `CompanyProfile`; `Interview` — сесія з кандидатом (`joinCode`, live, звіт). Звʼязок **1 Vacancy → N Interview**. Interview стартує зі статусу `AWAITING_CANDIDATE`. Підтверджену вакансію можна редагувати → знову `DRAFT` і повторний confirm. Hard-delete вакансії заборонено, якщо є linked interviews.

**Чому саме так:** Одна сутність змішувала профіль посади і сесію з людиною; HR потрібні кілька посад і кілька співбесід на одну анкету.

**Альтернативи:**
- Поле `kind` на `Interview` (анкета vs сесія)
- Лише UI-перейменування без нової таблиці
- 1:1 Vacancy–Interview

**Чому відмовилися:** `kind` накопичує технічний борг. UI-only не дає 1→N. 1:1 не покриває повторні співбесіди на ту саму посаду.

**Реалізація:** `docs/superpowers/specs/2026-07-08-hr-vacancy-interview-split-design.md`, `backend/prisma/schema.prisma`.

---

### PD-021. Глобальний профіль компанії + snapshot у вакансії

**Дата:** 2026-07-16  
**Статус:** прийнято

**Рішення:** `HrCompanyProfile` 1:1 з HR (культура, напрям, політики, формат, онбординг) через окремий company-prep. Без confirmed глобального профілю vacancy-prep недоступний. При формуванні вакансії універсальні поля **копіюються (snapshot)**; зміна глобального профілю не «плаває» в уже створених вакансіях.

**Чому саме так:** Універсальні відповіді не повинні повторюватись на кожній посаді; старі вакансії лишаються стабільним джерелом істини для match і звіту.

**Альтернативи:**
- Live-посилання на глобальний профіль без snapshot
- Усі поля лише на рівні вакансії (як до split)
- Кілька компаній / org на одного HR

**Чому відмовилися:** Live-link змінює історичні вакансії. Повтор на кожній вакансії — зайве тертя. Multi-company / org — поза MVP.

**Реалізація:** `docs/superpowers/specs/2026-07-16-company-global-profile-and-vacancy-snapshot-design.md`.

---

### PD-022. Self-service анкета кандидата («Моя анкета»)

**Дата:** 2026-07-09  
**Статус:** прийнято

**Рішення:** Кандидат один раз заповнює і підтверджує профіль у спеціальному контексті «Моя анкета»; той самий профіль перевикористовується для join / match / live. Contact bootstrap на старті prep: ПІБ, email (fallback з акаунту), phone (опційно після відмов).

**Чому саме так:** Кандидат не повинен проходити prep на кожне інтервʼю; контакти потрібні HR для заявок і запрошень.

**Альтернативи:**
- Prep профілю на кожному `Interview` (PD-005 as-is)
- Окрема таблиця `SavedCandidateProfile`
- Ручна форма без AI-чату

**Чому відмовилися:** Повторний prep — поганий UX. Окрема бібліотека — зайва модель для MVP. Форма без агента ламає єдиний prep-патерн продукту.

**Реалізація:** candidate dashboard / prep specs; `backend/src/utils/candidate-interview-kind.ts`, contact-bootstrap design.

---

### PD-023. Запрошення кандидата: dual-channel без SMTP

**Дата:** 2026-07-14  
**Статус:** прийнято

**Рішення:** Зовнішній канал (код / deep-link `/join?code=` / текст для копіювання) **без серверної пошти** + in-app `Invitation` за email. Макс 1 `PENDING` invitation на interview; join-by-code скасовує pending; decline не блокує код. Опційне `Interview.scheduledAt`.

**Чому саме так:** Зручний офлайн-обмін і in-app inbox без інфраструктури email у MVP.

**Альтернативи:**
- Тільки код (PD-014)
- SMTP / transactional email
- Публічний пошук співбесіди

**Чому відмовилися:** Лише код гірший UX для віддаленого найму. SMTP — зайва інфра. Публічний пошук — ризик витоку.

**Реалізація:** `docs/superpowers/specs/2026-07-14-candidate-invitation-dual-channel-design.md`. Розширює PD-014, не скасовує код.

---

### PD-024. Marketplace match: top-5 → заявка → Interview вручну

**Дата:** 2026-07-17 (top-5: 2026-07-20)  
**Статус:** прийнято

**Рішення:** Після confirm анкети кандидат бачить **топ-5** ranked `CONFIRMED` (і не hidden) вакансій: назва + % match (+ зарплата/формат за PD-028). Accept → `VacancyApplication` + `HrNotification`; **Interview створює HR вручну**. Ліміт: **1 PENDING** заявка на кандидата. Reject прибирає вакансію з пропозицій і підвантажує наступну з рейтингу. Кандидат **не** бачить культуру/філософію компанії; breakdown score — лише HR.

**Чому саме так:** Інверсія funnel поруч із join-by-code; HR контролює календар; приватність компанії і anti-gaming score.

**Альтернативи:**
- Sequential одна картка (перший design)
- Auto-create Interview після accept
- Показ повного профілю компанії кандидату
- Необмежена кількість PENDING заявок

**Чому відмовилися:** Одна картка гірше для вибору. Auto-Interview забирає контроль у HR. Повний профіль — витік. Багато PENDING — хаос у inbox.

**Реалізація:** vacancy-match + top-5 specs; `backend/src/routes/candidate-matches.ts`, `VacancyApplication`.

---

### PD-025. Critical / desired вимоги + детермінований score

**Дата:** 2026-07-21  
**Статус:** прийнято

**Рішення:** `requirements: { critical, desired }`; legacy `string[]` → усі desired. LLM оцінює assessments + `contextFit`; **бекенд** рахує score: `0.8 × requirementsFit + 0.2 × contextFit`, де `requirementsFit = 0.75×critical + 0.25×desired` (якщо обидва є). Явно `unmet` critical → **cap 69%**. `unknown` critical знижує score, але не вмикає cap. Той самий підхід у match і у фінальному звіті.

**Чому саме так:** Бізнес-правила стабільні поза моделлю; score пояснюваний; critical gap не маскується високим contextFit.

**Альтернативи:**
- LLM одразу повертає фінальний %
- Одна плоска вага для всіх вимог
- Cap лише в UI, не в формулі

**Чому відмовилися:** LLM-score нестабільний між прогонами. Плоска вага знецінює must-have. Cap лише в UI розʼїдеться з API/звітом.

**Реалізація:** `backend/src/services/match-score.ts`, prioritized-requirements design.

---

### PD-026. Arbiter як conductor (замість фіксованого порядку)

**Дата:** 2026-07-16  
**Статус:** прийнято

**Рішення:** Після human message / live start (debounce **1000 ms**) Arbiter повертає приховану команду: `START | ANSWER | NEXT_QUESTION | CLARIFY | CANDIDATE_QUESTIONS | COMPANY_ANSWER | WAIT | SUGGEST_END`. Orchestrator selective-run Company/Candidate у loop до `WAIT`/`SUGGEST_END` або `MAX_CONDUCTOR_STEPS = 100`. Стан conductor in-memory (не в БД). `START` лише після явного сигналу HR (вітання ≠ старт). HR бачить `room:arbiter-process` (`summaryUk`); кандидат — лише thinking.

**Чому саме так:** Фіксований порядок (PD-007) часто давав мовчання всіх агентів; Arbiter не міг оцінити відповідь у тому ж ході.

**Альтернативи:**
- Залишити фіксований Candidate → Company → Arbiter
- Паралельний fan-out усіх агентів
- Повна state machine у Prisma

**Чому відмовилися:** Фіксований порядок «мовчав». Паралельність ламає читабельність чату. Persist state — overkill для MVP (втрата при рестарті прийнятна).

**Реалізація:** `docs/superpowers/specs/2026-07-16-arbiter-conductor-design.md`, `backend/src/socket/orchestrator.ts`.

---

### PD-027. Candidate live: третя особа + confidence

**Дата:** 2026-07-16 / 2026-07-20  
**Статус:** прийнято

**Рішення:** Candidate Agent у live говорить **про кандидата** (не «я»). На `LiveMessage` — `candidateConfidence`: `CONFIRMED | INFERRED | UNKNOWN`. Бейджі видно обом сторонам. `unknown` → `needsHuman` і пауза; `inferred` — без паузи. Фінальний звіт враховує confidence у `risks`. Після перебивання HR Candidate відповідає на **усі** відкриті питання в одному повідомленні. Company на питання кандидата — факти з профілю або прохання до HR (`COMPANY_ANSWER`).

**Чому саме так:** AI — представник, не impersonation; HR бачить надійність відповіді; не губити питання після interrupt.

**Альтернативи:**
- Перша особа від імені кандидата
- Бінарний needsHuman без рівнів
- Confidence лише для HR

**Чому відмовилися:** Перша особа плутає «хто говорить». Бінарність не розрізняє висновок і прогалину. Приховування від кандидата зменшує прозорість спільної кімнати.

**Реалізація:** third-person / confidence / answer-all-open-questions specs.

---

### PD-028. Умови праці та компенсація на вакансії

**Дата:** 2026-07-20  
**Статус:** прийнято

**Рішення:** Per-vacancy `workConditions` + `compensation` у профілі; match-картка може показувати зарплату й формат; Company live відповідає на питання про умови з цих полів (або просить HR).

**Чому саме так:** Кандидату потрібні практичні умови для рішення; агент не повинен вигадувати цифри.

**Альтернативи:**
- Лише в глобальному профілі компанії
- Умови тільки в live-чаті від HR
- Окрема сутність Offers

**Чому відмовилися:** Умови часто відрізняються між посадами. Тільки live — погано для match UX. Offers — зайва модель.

**Реалізація:** `docs/superpowers/specs/2026-07-20-vacancy-work-conditions-design.md`.

---

### PD-029. Фінальний звіт: лише HR, правила recommendation + override

**Дата:** 2026-07-14 / 2026-07-22–23  
**Статус:** прийнято

**Рішення:** Після `POST /interviews/:id/end` (HR) генерується `FinalReport` (markdown). Кандидат **не** бачить AI-звіт. Немає PDF і regenerate у MVP. Critical unmet/unknown за замовчуванням блокують `HIRE`; score рахує бекенд. LLM може відхилитись лише з `overrideKind` + `overrideReason` (≥20 символів).

**Чому саме так:** Асиметрія найму; узгодженість з PD-025; soft skills / culture / red flags потребують явного винятку, не тихого ігнору правил.

**Альтернативи:**
- Звіт видимий кандидату
- LLM сам виставляє matchScore без правил
- Жорсткий бан HIRE без override

**Чому відмовилися:** Кандидатський доступ — окремий продуктовий і юридичний шар. Score від LLM розʼїдеться з match. Без override — нереалістично для edge cases.

**Реалізація:** report-page + recommendation-exception designs; `FinalReport` у schema.

---

### PD-030. HR-рішення після звіту + Dialogs HR↔кандидат

**Дата:** 2026-07-22  
**Статус:** прийнято

**Рішення:** Після звіту HR фіксує `ACCEPT | REJECT | ADDITIONAL_MEETING` → LLM draft листа → редагування → send у `Dialog` (1 на пару HR↔Candidate). Далі звичайний людський чат без агентів. «Додаткова зустріч» — лише лист; нове Interview вручну. Hide діалогу — per-user (`hrHiddenAt` / `candidateHiddenAt`), reappear на чужому повідомленні; hard-delete немає. Статус заявки синхронізується з листами (`DECLINED_BY_HR` / `ACCEPTED` / `ADDITIONAL_MEETING`).

**Чому саме так:** Людський контроль фінального рішення; єдиний канал відповіді кандидату; спільна історія без знищення.

**Альтернативи:**
- Автоsend листа без редагування
- Email замість in-app
- Auto-create follow-up Interview
- Hard-delete діалогів

**Чому відмовилися:** Автоsend ризикує помилковим тоном. Email — SMTP. Auto follow-up — календар без HR. Hard-delete ламає audit decision-листів.

**Реалізація:** hr-decision-dialogs + application-status-from-hr-letters + delete-dialogs designs.

---

### PD-031. Приховування вакансії (`hiddenAt`)

**Дата:** 2026-07-22  
**Статус:** прийнято

**Рішення:** `Vacancy.hiddenAt` ортогональний до `DRAFT`/`CONFIRMED` (не новий статус). Hidden вакансії зникають з match; блок hide при активних інтервʼю за потреби збереження процесу.

**Чому саме так:** Прибрати посаду з marketplace без delete історії та без плутанини зі статусом анкетування.

**Альтернативи:**
- Новий статус `ARCHIVED`
- Soft-delete всієї вакансії
- Залишити тільки CONFIRMED/DRAFT

**Чому відмовилися:** ARCHIVED змішує lifecycle prep і visibility. Soft-delete ускладнює звіти. Без hide — зайві match-пропозиції.

**Реалізація:** vacancy-hide design/plan.

---

### PD-032. Розширення LLM-провайдерів + retry у live

**Дата:** 2026-07-15 / 2026-07-22  
**Статус:** прийнято

**Рішення:** `LLM_PROVIDER=omlx|gemini|openai|cursor-acp`. Cursor ACP — child-process `agent acp`, session per complete (без офіційного ESM SDK). У live: `withLlmRetry` (max 3; timeouts не ретраяться) + HR-only `room:agent-retry` для зірваного кроку. Стрімінг LLM досі поза scope.

**Чому саме так:** Більше варіантів якості/вартості без зміни агентів; live не повинен «тихо» зависати на транзиєнтних помилках.

**Альтернативи:**
- Лише oMLX + Gemini (PD-019)
- LiteLLM як єдиний проксі
- Авто-retry без UI-сигналу HR

**Чому відмовилися:** Два провайдери тісні для demo/eval. LiteLLM — зайвий шар. Тихий retry ховає збій від HR у кімнаті.

**Реалізація:** `backend/src/llm/factory.ts`, cursor-acp + agent-retry designs.

---

### PD-033. Design system: Calm Slate + Teal

**Дата:** 2026-07-14  
**Статус:** прийнято

**Рішення:** Єдина візуальна мова UI — спокійний slate + teal accent, IBM Plex Sans; **без dark mode** у MVP. Кабінети HR і кандидата — sidebar layout.

**Чому саме так:** Продукт про спокійну співбесіду, не «AI glow»; одна система зменшує візуальний шум між екранами.

**Альтернативи:**
- Окремі стилі HR vs кандидат
- Dark-first
- Готовий UI-kit (Vuetify тощо) без власного токену

**Чому відмовилися:** Різні стилі дроблять бренд. Dark-first — не цільовий тон. Важкий UI-kit — зайва звʼязність для кастомного flow.

**Реалізація:** `docs/superpowers/specs/2026-07-14-calm-slate-teal-design.md`.

---

### PD-034. Docker Compose = лише Postgres

**Дата:** 2026-07-24  
**Статус:** прийнято

**Рішення:** У Compose — тільки Postgres 16 з volume і healthcheck. Backend (Express) і frontend (Vite) — локальні процеси; LLM — на хості.

**Чому саме так:** Швидший DX і простіший debug socket/LLM, ніж повний multi-container stack для щоденної розробки.

**Альтернативи:**
- Повний stack у Compose (як PD-018)
- Managed DB
- Усе без Docker

**Чому відмовилися:** Повний Compose повільніший для ітерацій UI/агентів. Managed DB — зайве для локального MVP. Без Docker — гірша відтворюваність БД.

**Реалізація:** `docker-compose.yml`. Уточнює PD-018.

---

### PD-035. Live = текст; READY → LIVE за presence

**Дата:** 2026-07-10 / 2026-07-13  
**Статус:** прийнято

**Рішення:** Джерело істини співбесіди — текстові `LiveMessage` (немає WebRTC / video / audio / recording). Статус `READY → LIVE` автоматично, коли в кімнаті є і HR, і кандидат (presence). Завершення — як PD-016 (ініціатива HR).

**Чому саме так:** Головна гіпотеза продукту — агентна дискусія в чаті; медіа — окремий продуктний шар. Авто-LIVE прибирає зайвий клік «старт статусу».

**Альтернативи:**
- WebRTC + запис
- Ручний «Почати співбесіду» для зміни статусу
- Запис лише audio

**Чому відмовилися:** Медіа різко ускладнює інфру й privacy. Ручний старт статусу дублює presence. Audio-only все одно потребує пайплайну запису.

**Реалізація:** live-human-chat / room UI designs; `maybe-transition-live.ts`.

---

### PD-036. Auth / tenancy tradeoffs MVP

**Дата:** 2026-07-03 / 2026-07-08  
**Статус:** прийнято

**Рішення:** Лише ролі `HR` | `CANDIDATE` (без ADMIN). HR без публічної реєстрації (seed); кандидат — self-register. Паролі — **SHA-256** (не bcrypt/argon) для локального MVP. JWT у `localStorage`, Bearer. Ізоляція ownership per user (HR бачить лише свої сутності); **немає** org/tenant model і кількох компаній на HR. CORS hardcoded під локальний frontend. PD-015 лишається: кандидат не може мати другу **активну** співбесіду.

**Чому саме так:** Швидкий локальний скелет з простою RBAC; multi-tenant org — окремий продукт.

**Альтернативи:**
- bcrypt/argon2
- Публічна реєстрація HR
- Org / workspace multi-tenancy
- HttpOnly session cookies

**Чому відмовилися:** Сильніший hashing і cookies — правильно для prod, але зайві для локального MVP. Публічний HR-signup без модерації. Org model — великий шар прав і білінгу.

**Реалізація:** `backend/src/auth/password.ts`, auth routes, middleware ownership checks.

---
